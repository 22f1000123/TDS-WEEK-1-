name: Cache Example

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: prime-cache-73ef1e4
        id: cache
        uses: actions/cache@v4
        with:
          path: ~/.npm
          key: cache-73ef1e4
          restore-keys: |
            cache- 


    

      - name: Check cache result
        run: echo "Cache hit = ${{ steps.cache.outputs.cache-hit }}"
